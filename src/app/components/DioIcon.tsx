import { DioIconImage, IDioIconImage } from "./DioIconImage"

export const DioIcon = ({ width, height }: IDioIconImage) => {
    return (
        <>
            <DioIconImage width={width} height={height} />
        </>
    )
}